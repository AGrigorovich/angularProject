import templateUrl from './designer.page.html';
import DesignerController from './designer.controller';

const DesignerComponent = {
	controller: DesignerController,
    template: templateUrl
}

export default DesignerComponent