import templateUrl from './builder.page.html';
import BuilderController from './builder.controller';

const BuilderComponent = {
	controller: BuilderController,
    template: templateUrl
}

export default BuilderComponent